services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: dc_admin
      POSTGRES_PASSWORD: change-me
      POSTGRES_DB: degreecomplete
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata: {}
